class CountryModel {
  String cdesc;
  String countryName;
  int noOfTours;
  double rating;
  String imgUrl;

  CountryModel(
      {this.countryName, this.cdesc, this.noOfTours, this.rating, this.imgUrl});
}
